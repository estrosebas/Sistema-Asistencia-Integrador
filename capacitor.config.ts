import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.estrosebas.sistemaasistencia',
  appName: 'sistema-asistencia',
  webDir: './out/renderer/'
}

export default config
